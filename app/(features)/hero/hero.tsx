import React from 'react';

import HeroContent from '@/app/(features)/hero/content';
import Header from '@/app/(features)/hero/header';

const HeroPage = () => {
  return (
    <>
      <Header />
      <HeroContent />
    </>
  );
};

export default HeroPage;
